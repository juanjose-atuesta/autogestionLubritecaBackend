services:
  backend:
    build: .
    ports:
      - "3001:3000"
    environment:
      - MONGO_URI=mongodb://mongo:27017/autogestionLubritecaDB
    depends_on:
      - mongo

  mongo:
    image: mongo:7
    volumes:
      - /var/lib/mongodb:/data/db
